# 采集间隔问题分析和解决方案

## 问题根源

### 1. 硬件端实际使用的配置
- 硬件端（`W601_SmartOffice/modules/reporter.py`）实际使用的是 `data.collect.interval`
- 默认值是 10000 毫秒（10秒）
- 虽然硬件端代码中有 `light_collect_interval` 和 `temp_humidity_collect_interval` 的存储，但实际并未使用

### 2. 前端保存的配置
- 前端只保存了 `light.collect.interval` 和 `temp.humidity.collect.interval`
- 没有保存 `data.collect.interval`（硬件端实际使用的）

### 3. 后端推送逻辑
- 之前只推送 `flame.detect.interval`
- `light.collect.interval` 和 `temp.humidity.collect.interval` 只保存到数据库，不推送到硬件

## 解决方案

### 已实施的修复

1. **前端修改**：
   - 保存配置时，同时保存 `data.collect.interval`（使用光照采集间隔的值）
   - 加载配置时，优先使用 `data.collect.interval`，如果没有则使用 `light.collect.interval`

2. **后端修改**：
   - 推送 `data.collect.interval` 和 `flame.detect.interval` 到硬件设备
   - `light.collect.interval` 和 `temp.humidity.collect.interval` 仍然只保存到数据库（假功能）

## 使用说明

### 修改采集间隔的步骤

1. 打开"系统配置"页面
2. 切换到"系统设置"标签页
3. 修改"光照采集间隔"的值（这个值会同时作为数据采集间隔）
4. 点击"保存配置"
5. 系统会自动：
   - 保存 `light.collect.interval` 到数据库（假功能）
   - 保存 `data.collect.interval` 到数据库（真功能）
   - 推送 `data.collect.interval` 到硬件设备
   - 硬件设备会立即应用新的采集间隔

### 验证修改是否生效

1. 查看后端日志，应该能看到：
   ```
   配置已推送到硬件设备: data.collect.interval=8
   ```

2. 查看硬件端日志（如果开启DEBUG），应该能看到：
   ```
   [CONFIG] 📥 收到配置更新: {'data.collect.interval': '8', 'timestamp': ...}
   [CONFIG] ✅ 更新数据采集间隔: 8秒 (8000毫秒)
   ```

3. 观察设备数据上报频率，应该按照新的间隔（如8秒）上报数据

## 注意事项

- **光照采集间隔**和**温湿度采集间隔**：只是保存到数据库，不会真正生效（假功能）
- **数据采集间隔**：会真正推送到硬件并生效（真功能）
- **火焰检测间隔**：会真正推送到硬件并生效（真功能）
- 修改"光照采集间隔"的值会同时影响硬件端的数据采集间隔

## 如果仍然不生效

1. **检查硬件设备是否在线**：
   - 确保设备已连接到MQTT服务器
   - 检查设备状态是否为"在线"

2. **检查MQTT连接**：
   - 确保后端MQTT客户端已连接
   - 检查后端日志是否有推送错误

3. **检查硬件端日志**：
   - 如果硬件端开启了DEBUG模式，查看是否收到配置更新消息

4. **手动测试**：
   - 可以通过MQTT客户端直接发送配置更新消息测试硬件端是否正常接收

