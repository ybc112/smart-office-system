# 🔥 消防疏散图快速修复指南

## ⚠️ 问题
图片无法显示，提示：`图片加载失败，路径: /images/image.png`

## ✅ 解决方案（任选一种）

### 方案1：手动复制文件（推荐，1分钟完成）

**步骤：**
1. 打开文件资源管理器，进入项目根目录：`E:\java毕设\智慧办公楼系统`
2. 在 `frontend/public/` 目录下，**创建** `images` 文件夹（如果不存在）
3. 将 `images/image.png` **复制**到 `frontend/public/images/image.png`
4. **重启前端开发服务器**（停止后重新运行 `npm run dev`）

**文件结构应该是：**
```
智慧办公楼系统/
├── images/
│   └── image.png          ← 源文件（已存在）
└── frontend/
    └── public/
        └── images/
            └── image.png   ← 需要复制到这里
```

### 方案2：使用现有图片（临时方案）

如果 `frontend/public/evacuation-map.png` 存在，代码会自动回退使用它。

### 方案3：使用环境变量

在 `frontend` 目录下创建 `.env.local` 文件：

```env
VITE_EVACUATION_MAP=/evacuation-map.png
```

## 📝 验证步骤

1. ✅ 确认文件已复制到：`frontend/public/images/image.png`
2. ✅ 重启前端开发服务器
3. ✅ 打开浏览器，访问前端页面
4. ✅ 点击"查看疏散图"按钮
5. ✅ 应该能看到疏散图了！

## 🛠️ 如果还是不行

1. 检查浏览器控制台（F12）的错误信息
2. 确认前端开发服务器正在运行
3. 尝试清除浏览器缓存后刷新页面

---

**最简单的方法：直接复制文件！** 🎯

