# 消防疏散图配置说明

## 问题说明
"查看疏散图"功能需要将图片文件放在正确的位置才能正常显示。

## 解决步骤

### 方法1：手动复制图片文件（推荐）

1. **创建目录**（如果不存在）：
   ```
   frontend/public/images/
   ```

2. **复制图片文件**：
   将 `images/image.png` 复制到 `frontend/public/images/image.png`

   **Windows PowerShell 命令：**
   ```powershell
   New-Item -ItemType Directory -Force -Path "frontend\public\images"
   Copy-Item "images\image.png" "frontend\public\images\image.png"
   ```

   **Windows CMD 命令：**
   ```cmd
   mkdir frontend\public\images
   copy images\image.png frontend\public\images\image.png
   ```

### 方法2：使用环境变量配置

如果您想使用其他路径的图片，可以在 `frontend` 目录下创建 `.env` 或 `.env.local` 文件：

```env
VITE_EVACUATION_MAP=/images/image.png
```

或者使用绝对URL：
```env
VITE_EVACUATION_MAP=http://example.com/path/to/image.png
```

## 验证

1. 确保图片文件位于：`frontend/public/images/image.png`
2. 重启前端开发服务器（如果正在运行）
3. 点击"查看疏散图"按钮，应该能看到图片

## 注意事项

- Vite 会将 `public` 目录下的文件直接服务到网站根目录
- 图片路径应该是 `/images/image.png`（相对于网站根目录）
- 如果图片加载失败，会显示错误提示信息

